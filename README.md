python -m venv venv
source venv/bin/activate      # Linux / Ubuntu
# o
venv\Scripts\activate         # Windows


Compatibilidad con Ubuntu

Funciona igual en Ubuntu, solo necesitas:

sudo apt install chromium-browser


O Chrome oficial.# Pdf-download-agent
